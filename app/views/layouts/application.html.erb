<!DOCTYPE html>
<html>
  <head>
    <title>Neighborhood Bulletin Board - Signin</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>


    <% if session.fetch(:user_id) == nil %>

      <div class="homepage-link">
        <a href="/user_sign_in">Sign in</a>
        <a href="/user_sign_up">Sign up</a>
        <a href="/">Home</a>
      </div>
    <% else %>
      <div class="homepage-link">
        <a href="/user_sign_out">Sign out</a>
        <a href="/bookmarks">My posts</a>
        <a href="/">Home</a>
      </div>
      <% end %>

      <div class="title">
        <h1>Your Neighborhood Bulletin Board</h1>
      </div>


    <%= yield %>
  </body>
</html>
